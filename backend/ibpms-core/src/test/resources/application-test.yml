spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    # Apuntando al entorno estático E2E (ADR-010 Enfoque E2E Estático)
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5433}/${POSTGRES_DB:ibpms_e2e}
    username: ${POSTGRES_USER:ibpms}
    password: ${POSTGRES_PASSWORD:ibpms_e2e_pass}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: false
    change-log: classpath:db/changelog/db.changelog-master.yaml
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5673}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6380}

# Disable context-path for integration tests (tests use full absolute paths)
server:
  servlet:
    context-path: ""

# Disable Camunda auto-deploy, job execution and external task client in tests
camunda:
  bpm:
    client:
      disable: true
    deployment-resource-pattern: ""
    job-execution:
      enabled: false
    database:
      schema-update: false

# Security: use a static JWT secret for tests
ibpms:
  pii:
    encryption:
      key: ibpms-test-aes256-encryption-k32
  clamav:
    url: http://localhost:3310
    timeout-ms: 5000
  webhook:
    security:
      mode: HMAC
      hmac-secret: test-hmac-secret-key-for-webhook-validation
    payload:
      max-size-bytes: 10485760
    pre-triage:
      process-definition-key: Process_PreTriaje_Intake
  azure:
    storage:
      connection-string: DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://127.0.0.1:10000/devstoreaccount1;
      container-name: ibpms-docs-test
  security:
    jwt:
      secret: UAT_SUPER_SECRET_KEY_THAT_IS_VERY_LONG_AND_SECURE_982374982374892374

# App-level properties for tests
app:
  storage:
    path: ./uploads-test
  azure:
    storage:
      connection-string: DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://127.0.0.1:10000/devstoreaccount1;
      container-name: ibpms-docs-test

jwt:
  secret: changeme-this-must-be-at-least-32-chars-for-hs256-signature!!
  expiration: 3600
