# 🧠→🕵️ Handoff: Arquitecto Líder → QA E2E
# T-20.3: Re-certificación de Regresión J-04 (Erradicación de Timeouts)

**Emitido por:** 🧠 ARQUITECTO LÍDER
**Destinatario:** 🕵️ QA - E2E
**Fecha:** 2026-05-12
**Sprint:** 7 — Iteración 7.1
**Prioridad:** 🔴 Alta
**Dependencia:** Frontend (T-20.2 completada)

---

## 1. Resumen Ejecutivo (El "Por Qué")
Tras la mitigación de los falsos positivos en capa de red (404s) en el Handoff T-20.1, el Agente Frontend acaba de reportar la finalización de la T-20.2, erradicando los bloqueos del DOM (Timeouts) en el DataGrid de `Workdesk` y `Kanban`. El objetivo de este Handoff T-20.3 es **re-ejecutar exclusivamente** los clusters E2E fallidos y certificar formalmente que las promesas asíncronas han sido destrabadas y los Timeouts mitigados en la capa UI.

---

## 2. Definición de Hecho (DoD - AP-02)
- ✅ Los tests de UI (`Phase1-Workdesk`, `Smoke-J04-Operario`, y `US002-Workbox-Kanban`) son ejecutados mediante Playwright.
- ✅ Los logs de Playwright confirman que los Timeouts (exceso del límite de 30s de espera visual) ya no ocurren en las vistas de DataGrid.
- ✅ Ley Global 4 (Inmutabilidad de Aserciones): Ningún expect de negocio fue alterado para forzar el estado "Green".
- ✅ Se genera y publica un reporte de estado final de la regresión confirmando la superación de los Timeouts P0.

---

## 3. Contexto Mandatorio (ADRs y Leyes Globales)
*   **Ley Global 4:** Excepción denegada. En esta fase de recertificación **está estrictamente prohibido** modificar cualquier código fuente (Backend, Frontend o E2E). Esta fase es puramente de ejecución y reporte.
*   **Regresión J-04:** Recordar que los fallos funcionales `403/500` detectados previamente en `us036-kill-switch-break-glass.e2e.spec.ts` son **Deuda Funcional Backend** y NO representan un bloqueo para validar que el DOM ya no sufra Timeouts.

---

## 4. Dependencias Técnicas Previas
- Handoff **T-20.2** (Frontend) implementado: El build local está actualizado con los bloques `finally` que aseguran que `isLoading = false` en `Workdesk.vue` y `KanbanView.vue`.
- Entorno Zero-Mock activo y escuchando en el puerto base.

---

## 5. Plan de Acción (Action Plan)
1.  **Aislar tests afectados:** Identificar los specs exactos de `Workdesk` y `Kanban` que previamente arrojaron Timeout.
2.  **Ejecutar pruebas dirigidas:** Correr `npx playwright test` especificando los archivos clave.
3.  **Auditar renderizado:** Validar en los resultados si los elementos (como tablas y botones de acción) ahora son encontrados exitosamente en el DOM sin superar el límite de espera.
4.  **Reportar Veredicto:** Redactar un resumen confirmando si el P0 de estabilización visual ha sido resuelto y cuántos tests cambiaron de `FAIL (Timeout)` a `PASS`.

---

## 6. Estrategia de Testing (Validación)
La validación se realizará examinando los reportes por consola o HTML de Playwright generados durante la ejecución de este handoff. Un test que falle por lógica de aserción (ej. valores incorrectos en la DB o fallos 403) es aceptable; un test que falle por **Timeout waiting for element** será considerado una falla en la mitigación de la UI.

---

## 7. Agnostic Handoff Payload (Copia y Pega esto al Agente QA)
```text
Asume el rol de 🕵️ QA - E2E.

ANTES DE HACER CUALQUIER COSA, lee obligatoriamente estos archivos en este orden exacto:

1. cat .cursorrules
2. cat ibpms-platform/.agents/skills/qa_playwright_zero_mock/SKILL.md
3. cat ibpms-platform/.agents/skills/architect_handoff_protocol/SKILL.md
4. cat ibpms-platform/.agentic-sync/T-20.3_QA_E2E_Timeouts_Recertification_Handoff.md

TU MISIÓN:

1. Re-ejecuta exclusivamente los specs E2E de Playwright que fallaron por Timeout durante la regresión J-04 (específicamente los relacionados con `Phase1-Workdesk`, `Smoke-J04-Operario` y `US002-Workbox-Kanban`).
2. Analiza los resultados: Tu métrica de éxito NO es lograr que todos pasen, sino validar si las mitigaciones del Frontend erradicaron los errores de tipo "Timeout" al esperar el renderizado del DOM en los DataGrids.
3. Recuerda que los fallos 403/500 en RBAC son deuda del Backend; tu foco son los fallos de interfaz asíncrona.
4. Entrega al Arquitecto Líder un reporte confirmando si el estado de carga (`isLoading`) ya no bloquea los tests.

REGLAS INQUEBRANTABLES:
- LEY GLOBAL 4 (Inmutabilidad): En esta misión TIENES ESTRICTAMENTE PROHIBIDO modificar el código fuente de los tests, el Frontend o el Backend. Tu labor es puramente investigativa y de ejecución (Run & Report).
```
