# URL del Webhook del Backend iBPMS (API Gateway o IP interna de la VM Backend)
# En producción (10.0.x.x o a través de DNS interno):
IBPMS_WEBHOOK_URL=http://ibpms-gateway:8080/api/v1/inbound/rpa-webhook
